# Arquivos grandes que não devem ir para o GitHub (limite de 100MB por arquivo)
*.zip

# Pasta de vídeos ORIGINAIS (não comprimidos).
# O site usa as versões comprimidas em images/ancore/videos/compactados/
ancore_site/html_template/videos/

# Vídeo institucional local — arquivo pesado, não versionar (hospedar separadamente)
ancore_site/html_template/images/ancore/videos/compactados/ancore_2024.mp4
ancore_site/html_template/images/ancore/videos/compactados/Ancore.\[2026\].webm

# Temporários / sistema
Thumbs.db
.DS_Store
